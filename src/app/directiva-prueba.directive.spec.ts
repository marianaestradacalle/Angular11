import { DirectivaPruebaDirective } from './directiva-prueba.directive';

describe('DirectivaPruebaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaPruebaDirective();
    expect(directive).toBeTruthy();
  });
});
